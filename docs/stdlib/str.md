# str.z

```
lines("a\nb\nc")            // ["a", "b", "c"]
slugify("Hello World!")     // "hello-world"
truncate("long text", 5)    // "lo..."
strip_tags("<p>Hi</p>")     // "Hi"
escape_html("<script>")     // "&lt;script&gt;"
to_csv([1, 2, 3])           // "1,2,3"
```
