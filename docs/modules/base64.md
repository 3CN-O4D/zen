# Base64 Module (`base64`)

Base64 encoding and decoding.

```zen
base64.encode("hello")        // "aGVsbG8="
base64.decode("aGVsbG8=")     // "hello"
base64.url_encode("hello")    // URL-safe encoding
base64.url_decode("aGVsbG8")  // URL-safe decoding
```
