# Decimal Module (`decimal`)

High-precision decimal arithmetic.

```zen
let d = decimal.Decimal("0.1")
decimal.getcontext()
decimal.setcontext(ctx)
```
Constants: `ROUND_HALF_UP`, `ROUND_DOWN`, `ROUND_CEILING`, `ROUND_FLOOR`, etc.
