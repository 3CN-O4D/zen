function scrape_links(url) {
    go url
    wait for "body"
    let page_title = text("title")
    let links = attrs("a", "href")
    print "Found " + len(links) + " links on: " + page_title
    return links
}

let links = scrape_links("https://example.com")
for link in links {
    print link
}
